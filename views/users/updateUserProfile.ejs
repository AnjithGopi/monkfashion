<%-include('../layouts/headerUser')-%>
<%- include('../layouts/mobileHeaderUser')-%>

    
    <main class="main">
        <div class="page-header breadcrumb-wrap">
            <div class="container">
                <div class="breadcrumb">
                    <a href="/home" rel="nofollow">Home</a>
                    <span></span> Profile
                    <span></span> Update Profile
                </div>
            </div>
        </div>
        <section class="pt-150 pb-150">
            <div class="container">
                <div class="row">
                    <div class="col-lg-10 m-auto">
                        <div class="row">
                            
                           
                            <div class="col-lg-12">
                                <div class="login_wrap widget-taber-content p-30 background-white border-radius-5">
                                    <div class="padding_eight_all bg-white">
                                        <div class="heading_s1">
                                            <h3 class="mb-30">Update User Profile</h3>
                                        </div>
                                      
                                        <form method="post"  name="addressForm" onsubmit="return validateForm()">
                                            <% if(user) { %>

                                            <div class="">
                                                <label for="name">Name :</label>
                                                <input type="text" required="" value="<%=user.name %>" name="name" placeholder="First name *" style="border-width: 1px; border-style: solid;border-color: black; font-weight: bold;" />
                                            </div>
                                            <div class="">
                                                <label for="phoneNumber">Phone Number:</label>
                                                <input type="tel" id="phone" name="phone"  value="<%=user.mobile %>"  pattern="[0-9]{10}"
                                                    placeholder="Enter your phone number *" required style="border-width: 1px; border-style: solid;border-color: black; font-weight: bold;" />
                                            </div>
                                            <div class="">
                                                <label for="altphoneNumber">Email:</label>
                                                <input required="" value= "<%= user.email %>" class="form-control square" name="email" type="email" style="border-width: 1px; border-style: solid;border-color: black; font-weight: bold;"/>
                                            </div>
                                            <div>
                                                <label for="dob">Date of Birth:</label>
                                                <input type="date" id="dob" name="dob">
                                            </div>
                                            
                                            <div class="">
                                                <button type="submit" class="mt-3" >
                                                    Update
                                                </button>           
                                             </div>
                                          
                                                <% }  else {%>
                                                
                                                <h3>Not Found</h3>
                                                <% }%>
                                            </form>

                                      
                                       
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
   
    <%-include('../layouts/footerUser')-%>


   



    <script>
        function validateForm() {
            let valid = true;
    
            const name = document.forms["addressForm"]["name"].value;
            const phone = document.forms["addressForm"]["email"].value;
            const phone2 = document.forms["addressForm"]["phone"].value;
            const cname = document.forms["addressForm"]["houseName"].value;
            const city = document.forms["addressForm"]["city"].value;
            const state = document.forms["addressForm"]["state"].value;
            const pincode = document.forms["addressForm"]["pincode"].value;
    
            // Validation for Name
            if (!name.trim()) {
                document.getElementById("nameError").textContent = "Name is required.";
                valid = false;
            } else {
                document.getElementById("nameError").textContent = "";
            }
    
            // Validation for Phone Number
            if (!phone.match(/^\d{10}$/)) {
                document.getElementById("phoneError").textContent = "Phone number must be 10 digits.";
                valid = false;
            } else {
                document.getElementById("phoneError").textContent = "";
            }
    
            // Validation for Alternate Phone Number
            if (phone2 && !phone2.match(/^\d{10}$/)) {
                document.getElementById("phone2Error").textContent = "Alternate phone number must be 10 digits.";
                valid = false;
            } else {
                document.getElementById("phone2Error").textContent = "";
            }
    
            // Validation for House Name
            if (!cname.trim()) {
                document.getElementById("cnameError").textContent = "House Name is required.";
                valid = false;
            } else {
                document.getElementById("cnameError").textContent = "";
            }
    
            // Validation for City
            if (!city.trim()) {
                document.getElementById("cityError").textContent = "City is required.";
                valid = false;
            } else {
                document.getElementById("cityError").textContent = "";
            }
    
            // Validation for State
            if (!state.trim()) {
                document.getElementById("stateError").textContent = "State is required.";
                valid = false;
            } else {
                document.getElementById("stateError").textContent = "";
            }
    
            // Validation for Pincode
            if (!pincode.match(/^\d{6}$/)) {
                document.getElementById("pincodeError").textContent = "Pincode must be 6 digits.";
                valid = false;
            } else {
                document.getElementById("pincodeError").textContent = "";
            }
    
            // You can add more validations for other fields as needed
    
            return valid;
        }
    </script>